// Extends Vitest's `expect` with jest-dom matchers (toBeInTheDocument, etc.)
// for every client test. Centralized here so individual tests don't import it.
import '@testing-library/jest-dom/vitest';
